version: "3.8"

services:
  intercept:
    build: .
    ports:
      - "5050:5050"
    environment:
      INTERCEPT_HOST: 0.0.0.0
      INTERCEPT_PORT: 5050
      INTERCEPT_LOG_LEVEL: INFO
    restart: unless-stopped
